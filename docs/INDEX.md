# docs/ — index & currency map

Every doc under `docs/`, its one-line purpose, and a **currency tag** so a new
reader knows what to trust. Tags:

- **CURRENT** — verified against code; load-bearing claims hold.
- **DECISION RECORD** — a frozen ADR in `decisions/`: the decision stands, the
  point-in-time system descriptions are not maintained for drift.
- **STALE — see note** — contains at least one claim the code contradicts; note says what.

Currency is verified by grepping each doc's load-bearing claims against the code, not
by trusting the doc's own "Status:" line. When you change behavior that a doc
describes, fix the doc **and** update its tag here in the same PR.

## Layout

```
docs/
├── architecture.md    the system doc — mental model + the locked contract
├── flows/             temporal traces (one task; dispatch → PR)
├── reference/         look-up tables (env vars)
├── runbooks/          operational procedures (shakedown, VPS deploy)
├── decisions/         FROZEN 2026-08-13 — historical ADRs (pipeline retired → speckit)
└── proposals/         FROZEN 2026-08-13 — historical direction drafts (pipeline retired → speckit)
```

Since 2026-08-13, behavior-changing work starts in `.specify/` (the speckit
pipeline — see `.claude/rules/speckit-workflow.md`); specs under
`.specify/specs/` are the living direction+execution artifacts. The
`decisions/` and `proposals/` sections below are frozen history.

## System

| Doc | Purpose | Currency |
|---|---|---|
| [`architecture.md`](./architecture.md) | **Start here.** Part I: the one-sitting mental model (five layers, two chains, the heartbeat, one task's journey, where state lives). Part II: the **locked contract** — per-layer contracts, the invariants (incl. grounded cognition, auto-heal), testability/replaceability, the question tree, the code map. | **CURRENT** — written 2026-07-13 (merged from `how-it-really-works.md` + `architecture-layers.md`), re-verified 2026-07-28. *Updated 2026-08-10*: layer-1 allowed-call list now names `delivery/repo.py` (the create_repo/delete_repo pair — a pre-existing omission). *Updated 2026-08-13*: layer-1 allowed-call list adds `intake.py` (`file_intake`, single-intake-doorway P1). Load-bearing claims checked against main during the docs audits; change history in git. *Updated 2026-08-17*: layer-1 map row says console, not dashboard — the legacy server-rendered dashboard routes 302 to `/console` (#549, one operator surface). *Updated 2026-08-18*: layer-1 intake entry adds the readiness-grading verbs `regrade_intake`/`grade_backlog` (spec 009 universal issue adoption — grade labels only, never dispatch). *Updated 2026-08-18*: spec 008 shrink — the investigating/firming lifecycle and the host planning-cognition chain (decomposer/firming/research/planner/checklist) are REMOVED; one execution path (the worker's speckit run); layer-3 roster and the heartbeat diagram updated to match. *Updated 2026-08-19*: spec 011 — the worker's OpenHands SDK is retired; the layer-5 replaceability invariant names the agent-drive seam (the runner's zero-dep ACP client + agent command), not `ACPAgent`. *Updated 2026-08-21*: the done-gate paragraph records that clause decomposition admits repository behavior only — delivery mechanics (ship shape, PR count, target branch, merge state, issue/PR closure) are dropped at step 1a of the evaluator prompt and never become clauses. *Updated 2026-08-22*: spec 010 P1 — Part I gains the **canonical vocabulary table** (work item / Definition of Ready / saga / Unit of Work / task graph / merge queue; FR-007, this table is now normative) and the **single-writer-per-project** section: at most one goal dispatches per project, and the hold is DERIVED (first non-terminal goal by age), never a stored lock. *Updated 2026-08-22*: the code map's `quality/` row drops `eval_judge`/`evals` — both modules were deleted as orphans (nothing but their own tests imported them). *Updated 2026-08-22*: spec 010 US3 — the gate list gains the **declared-scope gate** (a `[P]` increment's diff must stay inside the file scope its task graph declared; always-hard, zero-LLM, `loom/declared_scope.py`). #617 — "generated views are never read back" is now TRUE in code, not only stated: the eight lazy view-ingest read paths are deleted, the pre-existing markdown is ingested once by `goal/store/view_migration.py` at store construction, steering enters only through `steer_goal`, and `tests/test_views_never_read_back.py` guards it structurally. and the **planned fan-out** section (concurrent `[P]` lanes behind `DEVCLAW_FANOUT`, serial merge-queue integration onto the goal branch). 2 US2 — the vocabulary table's saga row names the five authoring slots, and a new **"a saga is authored against a schema"** section states the fill-or-be-rejected rule, the empty-vs-absent distinction, and the FR-009b size bound. 0 US3 — the gate list gains the **declared-scope gate** (a `[P]` increment's diff must stay inside the file scope its task graph declared; always-hard, zero-LLM, `loom/declared_scope.py`). #617 — "generated views are never read back" is now TRUE in code, not only stated: the eight lazy view-ingest read paths are deleted, the pre-existing markdown is ingested once by `goal/store/view_migration.py` at store construction, steering enters only through `steer_goal`, and `tests/test_views_never_read_back.py` guards it structurally. *Updated 2026-08-23*: spec 010 P3 shipped — the vocabulary table's parallel-safety and integration rows drop their "not built" markers and name `loom/declared_scope.py` / `loom/merge_queue.py`; fan-out itself is behind `DEVCLAW_FANOUT`, default off. and the **planned fan-out** section (concurrent `[P]` lanes behind `DEVCLAW_FANOUT`, serial merge-queue integration onto the goal branch). *Updated 2026-08-23*: spec 013 — the gate section gains **materialization**: the host stages and commits whatever the agent left the moment its run ends, so the change is a `pre_run_sha..post_run_sha` range that every gate, the change-size projection, the advisory checks and delivery all read; the always-hard `materialize` gate fails closed on a span it cannot determine, an empty span is an explicit no-change outcome counted as no progress, and a retry keeps the workspace instead of being rewound. |

## Flows

| Doc | Purpose | Currency |
|---|---|---|
| [`flows/task-execution.md`](./flows/task-execution.md) | Temporal trace of ONE task, every hop (node 1 waiter → node 2 devclaw-mcp → node 3 ephemeral sandbox), with a "fails if" rail. | **CURRENT** — audited 2026-07-13, re-verified 2026-07-28. All dispatch/settle symbols verified; Step I is `quality/review_gate` = single reviewer wrapped in the cognition-timeout degrade ladder. Change history in git. *Updated 2026-08-18*: spec 008 shrink — Step B dispatches a mechanical advance brief (`advance_brief.py`, zero LLM), no per-tick planner; goals are born `executing`. *Touched 2026-08-19*: amputation round 2 — event-callback consumer named honestly (console, not the retired dashboard). *Updated 2026-08-19*: spec 011 — Step D drives the agent via the runner's own `acp_client.py` (initialize → session/new → session/prompt), no OpenHands SDK. *Updated 2026-08-22*: spec 012 US1 — Step B's advance brief also carries what prior increments of the goal delivered (devclaw's own settlement facts, never the previous worker's summary). *Updated 2026-08-22*: spec 012 US2 — Step A rejects a saga with an unfilled slot before writing goal.yaml, and Step B's brief opens with the five-slot saga framing (re-sent in full, size-bounded). 3*: spec 013 — new **Step H2 (materialize the change)** between the runner's result and the gates; Step I reads that span rather than a fresh working-tree diff, and Step J publishes the judged head instead of discovering its own. |
| [`flows/delivery.md`](./flows/delivery.md) | How dispatches become PRs: the 3 delivery shapes (per-action legacy / goal branch / program), the dispatch cap, the Shape-3 reconcile step. | **CURRENT** — *audited 2026-07-13*: cap-recovery row now names both human verbs (`steer_goal`/`resume_goal`, #228/#238) and notes the cap block never auto-heals; the Shape-1 `pr_state` quote fixed to match `tick_settle.py` verbatim. Cap formula re-verified (algebraically identical to `tick_dispatch.py`). *Updated 2026-08-18*: spec 008 shrink — Shape 2 is the goal-branch standard (every `executing` goal; the checklist is gone), Shape 1 survives only for legacy NULL-lifecycle rows, programs run only on explicitly pre-planned DAGs, and the cap is `len(backlog) + 2`. *Touched 2026-08-19*: amputation round 2 — the `EVAL_EVERY` cadence claim replaced with the done-gate/on-demand evaluation that actually exists. |
| [`flows/autonomous-issue-pipeline.md`](./flows/autonomous-issue-pipeline.md) | End-to-end target-state flow: intake → readiness grade → dispatch → speckit scope → build → merge, with the label state machine and the human control points. | **PLANNED** — describes the P1–P3 arc (`specs/006-intake-readiness-gate`, `007-autonomous-issue-dispatch`, `008-speckit-execution-substrate`), **not yet built**. Not a trace of current behavior; do not trust as CURRENT until the specs land and the doc's banner is removed. *Updated 2026-08-18*: banner + Stage 1 note the shipped `specs/009-universal-issue-adoption` — the grade now accepts any existing open issue (`regrade_intake` format-tolerant, batch-capped `grade_backlog`); the door is no longer the only entrance. *Updated 2026-08-18*: spec 008 shrink — the P3 substrate is landed (executing-only lifecycle, no firming; label-routing still open); Stage-3 wording updated to match. *Updated 2026-08-22*: spec 012 US3 — Stage 0 records the filer's expected-increment claim and Stage 1 grades TWO independent axes on one cognition call (readiness + the extent check, the latter emitting `needs-sizing`); a new branch-point row covers a disputed or unrecorded extent. |

## Reference

| Doc | Purpose | Currency |
|---|---|---|
| [`reference/env-vars.md`](./reference/env-vars.md) | Single source of truth for every env var the runtime reads, grouped by purpose. | **CURRENT** — written/verified across the 2026-07-13 → 2026-07-30 tranches, re-verified 2026-08-06 (P3b planner cut: removed `DEVCLAW_THIN_PLAN` — the thin path is now the only long_lived path), updated 2026-08-13 (added `DEVCLAW_MAX_HOST_COGNITION` — host-cognition concurrency cap), updated 2026-08-17 (#554: `autodeploy` code default documented as conditional — app surface ⇒ on, library ⇒ off). Doc↔code parity is enforced by `tests/test_env_vars_doc_sync.py` (bidirectional) — the load-bearing guarantee this row exists for. Change history in git. *Updated 2026-08-19*: spec 011 — added `DEVCLAW_ACP_IDLE_TIMEOUT_S` (runner-side hang brake); engine-mode/runner rows de-OpenHands'd. *Updated 2026-08-22*: #595 — added `DEVCLAW_WORKSPACE_RETENTION_DAYS` + `DEVCLAW_WORKSPACE_RETENTION_DAYS_FAILED` (host-resource retention: devclaw now releases the workspace + toolchain volume it creates). *Updated 2026-08-22*: spec 010 US3 — added `DEVCLAW_FANOUT` (planned `[P]` fan-out, off by default) and noted that `DEVCLAW_MAX_CONCURRENT_PER_PROGRAM` doubles as the fan-out degree ceiling. |
| [`reference/intake-shape.md`](./reference/intake-shape.md) | The intake shape — how any ask (human or agent, any channel) enters devclaw via the `file_intake` MCP tool: fields, synchronous rejection rules, server-stamped provenance, the issue-URL receipt and its open/closed lifecycle, and the stage-1/stage-2 split (filing is unprivileged; dispatch stays with the authorized dispatcher). Enforcement is `devclaw/intake.py`; this page is the canonical narrative. | **CURRENT** — written 2026-08-13 with the P1 implementation of `proposals/single-intake-doorway.md`. |
| [`reference/intake-shape.md`](./reference/intake-shape.md) | The intake shape — how any ask (human or agent, any channel) enters devclaw via the `file_intake` MCP tool: fields, synchronous rejection rules, server-stamped provenance, the issue-URL receipt and its open/closed lifecycle, and the stage-1/stage-2 split (filing is unprivileged; dispatch stays with the authorized dispatcher). Enforcement is `devclaw/intake.py`; this page is the canonical narrative. | **CURRENT** — written 2026-08-13 with the P1 implementation of `proposals/single-intake-doorway.md`. *Updated 2026-08-22*: spec 012 US3 — the Fields table gains `expected_increments` / `increment_basis` (the filer's extent claim, recorded verbatim and never rewritten) and a new **Expected increments** section: grading validates the claim on a second, orthogonal axis, `needs-sizing` surfaces an unrecorded/unassessable/disputed extent for a human, and the count sizes the plan without ever selecting an execution shape (#600). |

## Runbooks

| Doc | Purpose | Currency |
|---|---|---|
| [`runbooks/live-shakedown.md`](./runbooks/live-shakedown.md) | Exercising the real pipeline (logged-in `claude` + docker) layer by layer, L1 single task → L5 abort. | **CURRENT** — *audited 2026-07-13*: every tool named in the steps exists on the MCP surface; L1–L5 descriptions match. *Updated 2026-07-19*: L2/L3 rewritten for the start_program → one-shot-goal alias (ADR 0003 stage 2b) — follow via `get_goal`/`tail_goal`, the child program appears in `list_programs` after the heartbeat dispatches the checklist. *Updated 2026-07-19 (post-shakedown)*: L2 workspace setup now requires a host-visible origin (goal-layer prep fetches it) and notes a /tmp file remote validates mechanics only — a green close needs a real pushable remote (sandbox can't push host file paths). *Updated 2026-08-16*: teardown now ARCHIVES the run (DB + log + manifest to `~/.devclaw/shakedown-runs/`) before cleaning — runs are the metrics series; replay via `DEVCLAW_DB=<archive>/devclaw.db`. *Updated 2026-08-17*: watch links point at `/console` — `/dashboard` now 302s there (#549, one operator surface). *Updated 2026-08-18*: spec 008 shrink — L2 is a one-shot goal on the advance loop (no investigate→firm→decompose, no child program to watch), L3/L5 follow via `get_goal`/`cancel_goal`, and the L2/L4 examples register a project (`project_id`, #520). *Updated 2026-08-19*: spec 011 — sandbox image is SDK-free (claude CLI + claude-agent-acp via npm); the importable-SDK failure row is now the `acp_client.py not loadable` row. *Updated 2026-08-22*: spec 012 US2 — the L4 `create_goal` example passes the three required saga slots. |
| [`runbooks/vps-waiter-deploy.md`](./runbooks/vps-waiter-deploy.md) | Deploying the OpenClaw waiter + devclaw to the VPS; the waiter's tool menu. | **CURRENT** — *audited 2026-07-13*: all menu tools exist; `resume_goal` added to the Goals line (#228). *Updated 2026-07-19*: `start_program` menu line marked as the deprecated one-shot-goal alias (ADR 0003 stage 2b — poll with `get_goal`, not `get_program`). *Updated 2026-08-10*: `delete_repo` added to the Tasks menu line. *Updated 2026-08-22*: spec 012 US2 — the grill→file flow carries the saga slots through to `create_goal`, which now requires them. |
| [`runbooks/project-reference-key-cutover.md`](./runbooks/project-reference-key-cutover.md) | Deploying the `project_id` dispatch cutover (#520): the CI-driven redeploy mechanism, the P3 startup backfill, the run-window-persists correction, and the waiter lockstep. | **CURRENT** — 2026-08-14: written from the first cutover deploy (P1 #522 · P2 #525 · P3 #528), all steps executed + verified live on the VPS. |
| [`runbooks/vps-waiter-deploy.md`](./runbooks/vps-waiter-deploy.md) | Deploying the OpenClaw waiter + devclaw to the VPS; the waiter's tool menu. | **CURRENT** — *audited 2026-07-13*: all menu tools exist; `resume_goal` added to the Goals line (#228). *Updated 2026-07-19*: `start_program` menu line marked as the deprecated one-shot-goal alias (ADR 0003 stage 2b — poll with `get_goal`, not `get_program`). *Updated 2026-08-10*: `delete_repo` added to the Tasks menu line. |
| [`runbooks/project-reference-key-cutover.md`](./runbooks/project-reference-key-cutover.md) | Deploying the `project_id` dispatch cutover (#520): the CI-driven redeploy mechanism, the one-shot P3 startup backfill, the run-window-persists correction, and the waiter lockstep. | **CURRENT** — 2026-08-22: written from the first cutover deploy (P1 #522 · P2 #525 · P3 #528), all steps executed + verified live on the VPS; step 4 updated for the #616 marker guard (the backfill now runs once per database, not every boot). |
| [`runbooks/devclaw-self-deploy.md`](./runbooks/devclaw-self-deploy.md) | The self-deploy cutover (spec 005): from-source `deploy/Dockerfile` → ghcr push → own `devclaw` compose project; the goal-safe volume-adoption order, the lifekit-stack companion change, rollback, and cold first-deploy. | **CURRENT** — 2026-08-16: first real cutover executed on lifekit-vps (workflow run 31967714716 @ `1a3da69`): images built from source + pushed to ghcr by SHA, `compose_devclaw-state` adopted in place with zero goal loss (32/32 rows), seams over `lifekit-shared` verified both directions, old monolith containers removed. Env-file ownership fixed same day (#541): default is the devclaw-owned `/srv/devclaw/.env` (template: `deploy/.env.example`). |

## Decision records (FROZEN 2026-08-13 — historical record only)

**The proposal→ADR pipeline is retired** (Denys's ruling, 2026-08-13 — full
speckit; see `.claude/rules/speckit-workflow.md`). ADRs 0001–0012 remain the
authoritative record of the decisions they froze, but nothing new lands here:
new direction is decided and recorded inside `.specify/specs/` specs.

| Doc | Purpose | Currency |
|---|---|---|
| [`decisions/0001-openhands-engine.md`](./decisions/0001-openhands-engine.md) | The founding decision: adopt a third-party coding-agent SDK as the execution engine; devclaw is thin orchestration. **The SDK it names (OpenHands) was replaced by the zero-dependency ACP runner in spec 011** — the agent-vs-isolation orthogonality rationale and why devclaw calls `docker run` itself. | **DECISION RECORD** — accepted 2026-06, frozen 2026-07-13 (was `architecture-v2.md`). The decision stands; system snapshots inside reflect their writing date. Current system: [`architecture.md`](./architecture.md). |
| [`decisions/0002-engine-mode.md`](./decisions/0002-engine-mode.md) | The OpenHands-vs-`claude_sdk` engine choice: current default, the switch procedure, and the data a switch requires. | **DECISION RECORD** — frozen 2026-07-13 (was `engine-decision.md`). **Its `claude_sdk` option no longer exists** — the engine was deleted 2026-08-22 (#613) and the switch it contemplated was overtaken by spec 011. Read as history; `DEVCLAW_ENGINE` now takes unset / `host` / `stub` only. |
| [`decisions/0003-goal-program-unification.md`](./decisions/0003-goal-program-unification.md) | Goal ↔ program unification: ONE `create_goal`, one dial (re-evaluation cadence — one-shot vs long-lived), one planning spine (grill→firm→decompose), shippable-release iterations, two-level sizing rule, fog-of-war scope map, checkpoint grill + auto-continue, hard cost backstops. Staged migration plan (PR-B → surface collapse → iterative loop). | **DECISION RECORD** — accepted 2026-07-19. Design locked; implementation staged — the "today" descriptions inside describe the pre-migration system and will drift as stages land. |
| [`decisions/0004-eval-workbench.md`](./decisions/0004-eval-workbench.md) | Eval-driven guardrail proportioning + the living eval workbench: stop hardening blind — measure with the existing instrument (`measure_passrate` + judge), rebalance Python guardrails toward context/prompt engineering, calibrate every shed to the WEAKEST target model (the 2×2; never delete on the Claude column alone), surface run history as a console "Evals" tab over an `eval_runs` table. Phased: Claude baseline → workbench MVP → judge-as-scorer → shed-candidate ID → local-model calibration + shedding LAST. | **DECISION RECORD** — accepted 2026-07-20 (graduated from `proposals/eval-workbench.md` under the spec lifecycle). Implementation phased; step-1 execution started 2026-07-20. **Step 2 amended 2026-07-21 by [ADR 0006](./decisions/0006-continuous-eval-projection.md)** (two-source outcome projection replaces the basket-only `eval_runs`). |
| [`decisions/0005-generic-sandbox-toolchain.md`](./decisions/0005-generic-sandbox-toolchain.md) | Kill the per-stack sandbox image family: one lean harness image; the toolchain is a project-declared fact (`.mise.toml` / `.tool-versions` / `global.json` / `package.json` engines) provisioned by mise in a fail-closed runner pre-step, cached in a per-project docker volume. Per-project `sandbox_image` override = escape hatch + migration bridge; greenfield goals get the declaration file as the first checklist item. | **DECISION RECORD** — accepted 2026-07-21 (graduated from the vault proposal `2026-07-11-generic-sandbox-project-declared-toolchain` under the spec lifecycle). Tranche COMPLETE 2026-07-21: gate passed live (finance-sentry#291 green on the lean image + mise path) and `Dockerfile.dotnet` is deleted — the "current family" snapshot inside reflects its writing date. |
| [`decisions/0006-continuous-eval-projection.md`](./decisions/0006-continuous-eval-projection.md) | Continuous evaluation as a projection of the live event stream: every live task settle writes an `eval_outcomes` row at the settle commit (zero extra tokens); basket runs ingest into the same table as `source=basket`; clean-cycle rate (locked boundary: self-healed pause = clean-but-reported) as the second headline metric; a mechanical window-close push report via the scheduled-edge owner (heartbeat today) → existing notifier; `measure_passrate` demoted to the experiment tool with an automatic 1-ticket post-deploy smoke. Amends ADR 0004 step 2 only. "night"→"cycle" vocabulary generalized 2026-07-22 (see the ADR's Naming note). Boundary amended 2026-08-07: an **idle** cycle (loop did no work — off/held/all-cancelled) is excluded from the clean-cycle rate so empty nights don't drift it. | **DECISION RECORD** — accepted 2026-07-21 (graduated from `proposals/continuous-eval-projection.md`; drafted, clarified, locked, and graduated the same evening). Tranche shipped 2026-07-22; idle-exclusion amendment 2026-08-07. |
| [`decisions/0007-gate-strictness-dial.md`](./decisions/0007-gate-strictness-dial.md) | Gate strictness becomes a per-goal dial (`trust\|strict`, default `trust`): dial-able gates (browser-E2E, adversarial-review) default to **advisory** — log loudly + surface in the PR + count in `eval_outcomes`, but ship rather than wedge; the human merge is the strict backstop. Three gates stay always-hard (test-integrity, delivery-trust, done-gate). Recalibrates — does not repeal — "loud failure over silent degradation". Impl shape: `Strictness` enum + ONE pure `(gate_id, verdict, strictness)→block\|advise` policy function, NOT mode-objects; Strategy seam reserved for the deferred judge-gate. Subsumes `DEVCLAW_GOAL_BROWSER_GATE_MODE`. | **DECISION RECORD** — accepted 2026-07-22 (graduated from `proposals/gate-strictness-dial.md`; drafted, clarified, locked, and graduated the same day). Tranche in flight 2026-07-22. |
| [`decisions/0008-console-operator-surface-p1.md`](./decisions/0008-console-operator-surface-p1.md) | Console operator surface **P1**: a read-only drill-down spine `node → project → goal → milestone(plan_key) → task` with active-shown/settled-folded disclosure at every tier + a layer-trace view — over projections that already exist (layer-1/dashboard only; no writes, no tick-path cognition). Milestone = `plan_key` grouping (a view, not an entity); P1 is read/navigate-only (mutation → P3). Net-new: `GET /node.json` vitals + surfacing `plan_key`/`milestone` in `_task_row` + the NODE/milestone/layer-trace frontend; reuses `GoalDetail`. Sized ≈6 PRs, end-of-week cap. | **DECISION RECORD** — accepted 2026-07-23 (graduated the P1 slice from `proposals/console-operator-surface.md`); **SHIPPED** PRs A–F (#357/#360/#361/#363/#364/#365). |
| [`decisions/0009-console-operator-surface-p2.md`](./decisions/0009-console-operator-surface-p2.md) | Console operator surface **P2**: the health enrichment + the **problem-lifecycle tracker**. Net-new `GET /problems.json` over the `problems` catalog (surfacing `issue_number`/`issue_state`) + a Problems screen rendering each problem *identified → filed → open → resolved* (self-issue-filing Stage 1 data); plus error-class (`failure_class`) breakdown + estimated token-spend on the existing Evals surface. Resolves §5.5 (fix stage shown **gated**, propose-only) + §5.6 (token spend **estimated**). Read-only, layer-1. Sized ≈2 PRs. | **DECISION RECORD** — accepted 2026-07-23 (graduated the P2 slice after P1 shipped); **SHIPPED** P2-A/P2-B (#367/#368). |
| [`decisions/0010-console-p3-structured-blocks.md`](./decisions/0010-console-p3-structured-blocks.md) | Console **P3.1 (§6)**: structured decision blocks. When the planner blocks `needs_answer`, its EXISTING cognition call also emits `{options:[{key,label,detail,steer}], recommended, allow_custom}`; options persist as a per-goal block-options doc (firming-unknowns precedent, no schema change), surface in `/goals/{id}.json`, and the console renders click-to-steer buttons reusing the EXISTING steer POST. No new LLM call, no new mutation/auth surface (§6a already solved). Resolves §6a–§6d. Layer-3 cognition change; blank-safe so absent options = today's behavior. Sized ≈2 PRs (P3.1a cognition+persistence+wire; P3.1b render). | **DECISION RECORD** — accepted 2026-07-23. **P3.2 (co-pilot chat + waiter-over-OAuth §4 + §5.3 chat-scope) deferred**, still in the proposal. |
| [`decisions/0011-branch-target-delivery-seam.md`](./decisions/0011-branch-target-delivery-seam.md) | **v1-helper P1**: the branch-target delivery seam. `dispatch_task` → queue → `deliver_change` gain optional `base_branch` (ahead-count/diff grounding + `gh pr create --base`) and `target_branch` (workspace prepped ON the pinned branch, goal-mode PR-reuse predicate widened to caller-pinned branches). Two loud-fail hardening rules from the tranche's invariant review: a pinned-target miss settles the task `failed`; a bogus base fails at dispatch, not downstream. Omitted params = byte-identical legacy behavior; goal layer untouched; delivery stays fail-closed (#183). | **DECISION RECORD** — accepted 2026-07-27 (graduated the P1 slice from `proposals/v1-helper-resurface.md`); tranche shipped starting PR #387. P2 (console "file a task" surface) + P3 (direct-path prep ergonomics) stay in the proposal, named-unsized. |
| [`decisions/0012-single-intake-doorway.md`](./decisions/0012-single-intake-doorway.md) | **The single intake doorway**: every devclaw-bound ask — human or agent, any channel — is first recorded via the low-privilege `file_intake` MCP tool (python-validated shape at the choke point, server-stamped provenance, `devclaw-intake` issue on the target registered project's repo, issue URL = the asker's durable receipt, loud failure = no fake receipts); execution admission stays a separate human-gated act by the dispatch tools, referencing the intake issue. Rejected alternatives frozen: raw `gh` filing (enforces nothing; mediating agents already hold MCP creds) and an internal pending queue (second backlog store). Issues = intent, SQLite = execution; no per-repo templates. | **DECISION RECORD** — accepted 2026-08-13 (graduated from `proposals/single-intake-doorway.md`, locked and scheduled the same day). P1 shipped: #513 + lifekit-stack#117. P2 (doorway unavoidable at dispatch; `Closes` vs `Refs` per goal mode) + P3 (push-notify asker; scorecard-gated auto-pickup) stay in the proposal, named-unsized. |

## Proposals (FROZEN 2026-08-13 — historical record only)

**The proposal→ADR pipeline is retired** (Denys's ruling, 2026-08-13 — full
speckit; see `.claude/rules/speckit-workflow.md`). Nothing new lands here and
no status line changes anymore. DRAFT proposals below were never locked; if
their work is picked up it starts fresh as a `.specify/specs/` spec (the
proposal is source material, not a binding direction). The tags below mirror
each doc's own status line as of the freeze.

| Doc | Purpose | Status |
|---|---|---|
| archived proposals | Graduated/abandoned drafts, kept for their narrative + clarify-step trail — see [`proposals/archived/`](./proposals/archived/): `eval-workbench` → [ADR 0004](./decisions/0004-eval-workbench.md), `continuous-eval-projection` → [ADR 0006](./decisions/0006-continuous-eval-projection.md), `gate-strictness-dial` → [ADR 0007](./decisions/0007-gate-strictness-dial.md), `ops-agent-problems-consolidation` **ABANDONED**. | history |
| [`proposals/self-issue-filing.md`](./proposals/self-issue-filing.md) | The **self-improving cycle** (GATHER→FILE→FIX→CLOSE): devclaw turns a *recurring* problem into a labeled GitHub issue on its own repo, and — later, gated — picks it up as a goal to fix through the existing gates; issues leave the board when fixed or aged-out (anti-accumulation). Dev-loop half of the ops-agent cut; rescues the dead O4 trend-repeat detector as the recurrence trigger. Sliced: **P1 file+close** (mechanical, zero-LLM, no self-merge) → **P2 fix** → P3 tighten. | **Stage 1 (FILE+CLOSE) LOCKED** — 2026-07-22; **Stage 2 (FIX) LOCKED** — 2026-07-23 (§3 self-merge = tiered by blast radius; P2 sliced propose-only, tiered classifier deferred to P2.1/P2.2; P3 unsized). Backlog #340. Latest amendments 2026-07-28 (O5 human-handoff `devclaw:pickup` intake; O1 FILE-revival threshold 3→2). Amendment history in git. |
| [`proposals/v1-helper-resurface.md`](./proposals/v1-helper-resurface.md) | **Re-surface the v1 task-runner helper** (bounded task → PR, no goal ceremony) — reconciles the helper→PoC drift + removes the `speckit-handoff-gap` wall. Grounded finding: the direct runner (`dispatch_task`) is fully wired and works; it's just **unsurfaced** and can only branch a fresh name off `main` → PR to `main`. Base=`main` lives in exactly 2 spots and the branch-reuse capability already exists (gated to `goal/…`); the seam was pre-planned (`delivery_strategy.py`, `PerActionStrategy`). Sliced **P1 = the branch-target delivery seam** (`base_branch` + `target_branch`, continue an existing branch, PR to a chosen base) → **P2 = console "file a task" surface + docs** → **P3 = direct-path workspace-prep ergonomics**. | **P1 GRADUATED → [ADR 0011](./decisions/0011-branch-target-delivery-seam.md)** — 2026-07-27 (tranche greenlit + shipped starting PR #387). The ADR is canonical for P1; the proposal keeps the narrative + clarify trail and remains the home of P2/P3 (named-unsized, owner Denys). |
| [`proposals/issue-driven-pipelines.md`](./proposals/issue-driven-pipelines.md) | **Issue-driven continuous development** — GitHub Issues as the control plane, devclaw as the engine. Two non-overlapping intake threads onto one engine: **goals** (durable, build-a-product) vs a **per-repo queue** (bounded units = labeled GitHub Issues → picked up → PR → closed). One source of truth split by *kind*: Issues canonical for **intent/backlog**, SQLite canonical for **execution state** (Issues have no CAS — never the transactional store). Governance = an **in-repo `.devclaw/` manifest** (opt-in, inert, portable — the repo lives without devclaw; presence = the allowlist) + thin webhook/subscription discovery. Generalizes the existing self-issue-filing pickup from the devclaw repo to any owned repo (first external = finance-sentry). **Deliberately split after an honest scope review (§0.5):** the grand control-plane vision (manifest, webhooks, multi-repo) is **DEFERRED — recorded, unscheduled** (revive only after the reliability wall is down; done early it would mass-produce the subtly-wrong PRs that got finance-sentry-ui cancelled). Only a small **NOW slice** is live work: **N1** collapse `problems`→Issues (source-of-truth hygiene) + **N2** make the existing self-issue-filing loop legible in the console. | **DRAFT (split NOW/DEFERRED)** — 2026-07-23; captured from a 4-message design conversation, then scope-reviewed. NOW = N1/N2 (bounded, out of lock scope). DEFERRED arc keeps 8 `[OPEN]`s (§7), mandatory only if revived. |
| [`proposals/console-operator-surface.md`](./proposals/console-operator-surface.md) | The console as a **live operator surface**, not a status board: the running node visible from the top, drilled down through `node → project → goal → milestone(slice) → task` with active-shown/settled-folded disclosure at every tier, a layer trace that catches errors in place, a health/self-evaluation dashboard (`eval_outcomes` + `cycle_reports` charts), a **problem-lifecycle tracker** (identified→filed→fixing→resolved), and a **co-pilot chat** that commands it all — over the *existing waiter*/OAuth (§4), via an intent API, not computer-use. Plus (§6) **structured decision blocks**: a `needs_answer`-blocked goal renders as options + a recommendation + custom-answer, click→`steer_goal` (generalizes firming's `answer_unknowns` to execution). Packaging/legibility-primary, with a `[OPEN] #7` lean toward a commandable daily driver (P2/P3 direction). | **P1 → [ADR 0008](./decisions/0008-console-operator-surface-p1.md) (SHIPPED); P2 → [ADR 0009](./decisions/0009-console-operator-surface-p2.md) (SHIPPED); P3.1 §6 → [ADR 0010](./decisions/0010-console-p3-structured-blocks.md) (graduated)** — 2026-07-23; all clarify items resolved. Reference mockup is a demo, not the target IA (§5.8). P1 read-hierarchy + P2 health/problems shipped; **P3.1 = §6 structured decision blocks** (ADR 0010, ≈2 PRs); **P3.2 = co-pilot chat + waiter-over-OAuth (§4) + §5.3 chat-scope** stays named-unsized, LOCKED-direction here. |
| [`proposals/observation-resolution.md`](./proposals/observation-resolution.md) | **Observation resolution** — one lifecycle for trends + problems, notify-only operation. Ground truth: trends are **write-only** (append-only `trends.md` + a Telegram post per fire; the `Proposed action` field is never read back — the live D3/H4 entries complain about it themselves), while problems already have the full loop (fingerprint → recurrence → Issue → propose-fix → close). Direction: route *actionable* trend output into `record_problem` (category `trend`, zero new LLM), let the existing self-issue-filing pipeline file/fix/close it, add ONE resolution-notification hook (Telegram pings on Issue close), and re-tier per-fire trend noise into the digest. Merge authority explicitly unchanged (self-issue-filing Stage-2 lock owns that). Sliced P1 intake+notify (~2 PRs) / P2 noise re-tier / P3 delegated. | **P1 LOCKED (direction)** — 2026-07-27 (same-day clarify: all 5 `[OPEN]`s resolved by Denys in conversation, recorded in §5 — `trend` category, digest-only for no-action trends, self-repo filing, min_cycles inherited from self-issue-filing O1 — default 2 since its 2026-07-28 amendment, harness-self included). P1 build greenlit; P2 named-unsized, shaped by O2. |
| [`proposals/console-legibility.md`](./proposals/console-legibility.md) | **Console legibility** — from a live dogfood walkthrough. One recurring failure at every level: the console renders IDs + class names, not what happened. Two threads: **A — tracing altitude** (a task is a dead ID; `ACPToolCallEvent` throws away its payload; timeline stages aren't drillable; and the fix is NOT "show more raw" — default to a **brief plain-language narrative**, keep the raw trace one click away; evals too) and **B — state honesty** ("active" undefined so dead projects read active + no archive verb; overview vs projects-page counts disagree 3-vs-6; the Problems page reads "~99 / 0 resolved" — a lifetime pile with no windowing or resolution sync = the "issues coming and coming" dread made literal). Headline constraint: a live-view "brief" must be **zero-token** (mechanical rollup, not tick-path cognition). Sliced: state-honesty CORRECTNESS is bug-exempt + already in flight via the `console-state-honesty` self-goal (out of lock scope); **P1 = tracing-altitude spine** (brief-by-default + expand-to-deep across task panel/activity/timeline), **P2 = project lifecycle** (define active + archive), **P3 = evals depth**. | **P1 LOCKED (direction)** — 2026-07-29 (same-day clarify: `[OPEN-1..3]` resolved — mechanical-rollup brief for live/idle views (zero-token, invariant-forced), phase/delivery default granularity with a rolling in-flight line, deep-trace retention deferred to #259; `[OPEN-4/5]` deferred to P2/P3). P1 = tracing-altitude spine, buildable. P2 (project lifecycle) + P3 (evals depth) named-unsized. State-honesty correctness half is live via the `console-state-honesty` self-goal. Next console P-series over ADRs 0008–0010. **Amended + re-locked 2026-08-07** (second walkthrough): Thread A #1 sharpened to a routed **task drill-in** (`/console/goals/:id/tasks/:taskId`, live-or-history) = **P1-A SHIPPED 2026-08-07** (#479); **P1-B = PLAN.md-as-spine SHIPPED 2026-08-07** (Plan tab renders the worker-owned PLAN.md read off the goal's live delivery branch, `GET /goals/{id}/plan.json`); **P1-C = plan-as-spine landing SHIPPED 2026-08-07** (Plan is the default tab; PLAN.md + the live task stream beneath it, top-to-bottom, tasks drill in); new **P4 = stream-tab consolidation** (8 tabs → ~4; Activity/Trace/Cognition → one altitude-tagged feed, Execution → the task page) direction-LOCKED with shape `[OPEN-6..8]`; PR #474 shipped the Thread A #2 payload-render. |
| [`proposals/foundation-checkpoint.md`](./proposals/foundation-checkpoint.md) | **Foundation checkpoint** (issue #432) — verify load-bearing `done_when` constraints (a required base dependency/framework, a required package name) EARLY, not at the final done-gate after huge sunk cost (finance-sentry-ui built 42 components on Tailwind, not the required ng-zorro, caught only at the terminal gate). Systemic, domain-general: **firming** derives `foundation_asserts` (`ItemAssert`-shaped predicates, grounded in REPOSITORY CONTEXT) — the cognition half that already runs; a **mechanical, zero-LLM check** after the first delivery (gated past `should_plan`) blocks `needs_answer` on a wrong foundation. Lifts the existing per-item `ItemAssert` machinery to the goal level. Sliced P1 (checkpoint + firming emit) / P2 (re-check torn-out foundation) / P3 (decomposer pins it to the first scaffold item). | **DRAFT** — 2026-07-29; 4 `[OPEN]`s (§5: fire-once flag home, check hook site, fire timing, firming cost) mandatory before LOCK. **No code before lock** (new mechanism, spec-lifecycle). |
| [`proposals/delivery-last-mile.md`](./proposals/delivery-last-mile.md) | **Delivery last-mile** — the "PR merges, I get told, the goal pursues" owner contract. Ground truth: auto-merge on gate-green exists and is ON in prod (`merge.py`, `DEVCLAW_GOAL_AUTOMERGE=1`), the failed-merge owner-ping exists (`tick_settle`), yet the 2026-07-28 morning still yielded two CONFLICTING PRs, a silently-unengaged merge path, and zero owner notification. Direction: every gate-green delivery resolves legibly to `merged \| left-for-owner(reason) \| skipped(reason)`; merge policy reconciles with the ADR 0007 trust-backstop premise; a "shipped overnight" PR ledger rides the ADR 0006 cycle report; branch hygiene via #393/#394 (independent mechanical fixes). Zero tick-path LLM. | **LOCKED (direction)** — 2026-07-28 (same-day clarify: all 5 `[OPEN]`s resolved by Denys, §5 — headline: O1 auto-merges gate-green *even with surviving advisory findings* under `trust`, amending the ADR 0007 backstop premise for automerge-on repos; done-gate + ledger become the net). P1 firmed ~2 PRs; companions #393/#394 land independently. |
| [`proposals/quality-gate-extraction.md`](./proposals/quality-gate-extraction.md) | **Extract `devclaw/quality/` as a standalone fork-and-own tool** — the fail-closed adversarial review gate (no verdict ⇒ no approval) as a public repo any project can run on a PR, kunchenguid-fleet-style (wheelhouse proves the `CLAUDE_CODE_OAUTH_TOKEN`-in-Actions distribution pattern). Ground truth: the package pre-planned this (own README/prompts; three leaf seams pinned by `test_quality_package.py`; one devclaw call site). Admission ticket = the **second-consumer test** (the goalclaw fold-back precedent: no lock unless O6 names a live non-devclaw consumer). Sliced **P1 = repo + review core standalone via CLI + one wired consumer (~2–3 PRs)** → P2 GitHub Action wrapper → P3 dependency inversion (devclaw consumes the package) + fleet registration (the new repo becomes a devclaw-maintained project). devclaw untouched in P1; `gate_policy.py` (ADR 0007 dial) stays devclaw-side. | **LOCKED (direction)** — 2026-07-28 (same-day clarify: all 8 `[OPEN]`s resolved by Denys, recorded in §5 — repo = `lifekit-hq/portcullis` (MIT), CLI-first, OAuth-default + explicit API-key opt-in, freeze-embedded until P3, **loom moves entirely** (Denys overrode absorb-two-modules), second consumer = finance-sentry, review core only, register-at-P1-close + human-gated). P1 firmed ~2–3 PRs; tranche scheduling stays Denys's call. |
| [`proposals/overnight-autonomy-no-block-wait.md`](./proposals/overnight-autonomy-no-block-wait.md) | **Overnight autonomy — a goal never blocks-and-waits during the run window.** The core owner pain: every night a blocker (review-gate crash, env failure, package-lock) fires at 10pm and parks the goal until morning. Not a defect — Tranche-0's fail-loud / block-and-ping posture is right for *finding* walls, wrong for an *unattended* operator. Direction: during the run window a goal never blocks-and-waits; it retries → abandons-the-item → continues, surfacing abandoned items in the morning report. Fail-closed gates untouched (nothing unverified ships); the change is skip-item-and-continue instead of park-for-human. Defines **clean run** as the measured target: *did anything reach the owner before morning?* Lever #1 of three from the 2026-07-30 clean-run conversation (class-kills + goal-selection are the other two, done directly as hardening). Sliced P1 = `one_shot` checklist goals + the existing cycle-report surface. | **DRAFT** — 2026-07-30; 6 `[OPEN]`s (block-kind policy, retry budget, `one_shot`-vs-`long_lived` scope, circuit-breaker relationship, morning-summary surface, all-abandoned case) mandatory before LOCK. **No code before lock** (behavior change to the escalation contract, spec-lifecycle). |
| [`proposals/cognition-demolition.md`](./proposals/cognition-demolition.md) | **Cognition demolition — collapse 8 control-plane cognition boundaries to 1 (the done-gate).** Durable plan-state moves to a **plain worker-owned `PLAN.md` file committed to the target repo** (the 2026-08-05 pivot — *thinner* than the earlier wayfinder GitHub-issue machinery, now mooted). The control plane never re-plans and never reads the plan back: it mechanically dispatches "advance the goal, keep `PLAN.md` current" + the surviving done-gate — so the planner LLM call dies as a *consequence*, not a goal. Held against the thesis (durable pursuit that survives crashes, plans without hallucinating): plan-state migration first, call-removal second. Same cut list, same **~2,500 net LOC deleted, ~75% of it prompts.** Sliced **P1 = cut the per-tick mid-flight evaluator (SHIPPED #456)** → **P2** the worker `PLAN.md` skill → **P3** planner cut + tick rewire (re-measured vs the 2/2 baseline) → **P4** decomposer/firming/gates/summarizer. | **PIVOTED + LOCKED (direction)** — 2026-08-05 (thin `PLAN.md`, not gh-issues; O2/O8 mooted, O3/O4 survive small). P1 shipped; the merged P2 host-side foundation (#457/#458/#459) is now inert. **P3 planner-cut merge gated on re-running the pass-rate baseline** (ADR 0004/0006 instrument). |
| [`proposals/compounding-experiment.md`](./proposals/compounding-experiment.md) | **The compounding experiment — does a durable goal build on itself across nights?** The thesis (durable-goal layer + grounded done-gate is the moat over a one-shot-equivalent worker) rides on multi-night **compounding** that has never been observed (the 2026-08-05 scale test was explicitly one-shot). Tests one falsifiable claim: a goal whose `done_when` needs ~5–10 nights shows a **monotonically non-decreasing** count of satisfied criteria, **never reverts** one, and **closes** via the done-gate. Metric = the app's `done_when` encoded as an **executable checklist** (one deterministic check per feature) run against repo HEAD each night → a per-night criteria vector; compounding = non-decreasing green-count → all-green, anti-signals = churn (green→red) / plateau. Target = **"Ledger", a .NET minimal-API + Angular expense tracker** (10 layered, mechanically-checkable features). Sliced **P1 = design (LOCKED) + instrument** (P1-B checklist+spec in-repo, P1-C compounding scorecard projection — both from the box) → the nightly run is Denys's (live box) → P2/P3 = console view · second app · one-shot contrast. | **P1 LOCKED (direction)** — 2026-08-07 (captured+clarified+locked one session: success = monotonic-progress-to-close, no baseline; goal = .NET+Angular "Ledger"; **grading checklist scorecard-side & hidden** from the worker — prose SPEC.md in-repo, checks in `evals/ledger_checklist/` = trust-input/verify-output; §6 `[OPEN-1..4]` resolved, `[OPEN-5]` one-shot counterfactual deferred-to-P2). P1-B (spec+hidden checklist) + P1-C (compounding scorecard) buildable from the box; nightly run is Denys's on the live box. **Amended 2026-08-08** — substrate gap fixed: long_lived goals now accumulate on `goal/<id>` (was reset-to-main amnesia); scorecard must grade the goal branch, not main; auto-merge bounding deferred as a named fast-follow. |
| [`proposals/sdlc-execution-pipeline.md`](./proposals/sdlc-execution-pipeline.md) | **The SDLC execution pipeline — reaching senior-team behavior.** The north-star frame: devclaw should execute like a real-world software team — do as much per PR as a capable model can, but sized/sequenced like a disciplined senior team (coherent feature per PR, verified, merged before next), NOT hundreds of one-liner PRs (a temp crutch) and NOT one giant unreviewable dump (ledger night-1's 17k-line 10-feature PR). Hypothesis: senior behavior = **well-grained prompts + model reasoning + Python guardrails, as a logical pipeline.** Load-bearing principle: **assign each decision to the layer that can ENFORCE it** (invariants→Python fail-loud, judgment→reasoning, standard→prompts — "small PRs" as a prompt got ignored). Maps the SDLC loop (ground→**slice**→implement→**verify**→**review**→**merge**→iterate) onto devclaw's existing stages; senior-vs-junior lives at stage 2 (slice) + 4 (definition-of-done); trap = don't put judgment in rigid Python (the line-counter). Sliced **P1 = accumulation seam (goal-branch, PR #486)** → P2 slicing heuristic+guardrail → P3 per-slice DoD + auto-merge reconcile + OOM-admission floor. | **LOCKED (direction)** — 2026-08-08 (captured + all 5 `[OPEN]`s clarified/resolved in one session: slicing enforcement via prompt+milestone-guardrail, per-slice DoD = existing gate on a bounded slice, **accumulate → merge at done-gate** (auto-merge skip re-keyed to on-goal-branch), bound is config revisited on model-release, slicing lives in worker PLAN.md (pull)). **P1 SHIPPED (#486)**; **P2 firmable** (slicing prompt + trust-dial guardrail + auto-merge re-key, ~2 PRs); P3 named (OOM floor; incremental-merge-to-main as the north-star evolution). |
| [`proposals/observability-maxout.md`](./proposals/observability-maxout.md) | **Observability max-out — watch the watcher, remember the metrics, schedule the evals.** From the 2026-08-12 grounded audit: in-loop instrumentation is strong (trace net, problems catalog, live `eval_outcomes`, clean-cycle grading) but nothing watches the daemon itself, metrics are snapshot-only, alerting is binary/best-effort, evals are never scheduled, and diagnosis surfaces are thin or lie (#494–#496). Carries a grounded ops-agent verdict on live evidence (daily zombie O1 incidents on cancelled goals, run-window-blind O3, blind to a dead daemon): keep the sibling process, repurpose it as the mechanical zero-LLM watchdog. Sliced **P1 = heartbeat freshness + dead-man O5 + surface honesty (~5 PRs, firmed)** → P2 scorecard snapshots + threshold pings → P3 scheduled evals + notify hardening. | **LOCKED (direction)** — 2026-08-12 (drafted, clarified, and locked the same session; all 5 `[OPEN]`s resolved: O5 ping-only first, detectors get hygiene-now/strip-decided-after-the-ledger-watch, P2 = snapshots only with thresholds as P2.1 after ~2wk baseline, weekly capped pause-aware real-eval burn, full P3 in). Tranche unscheduled — sequencing stays Denys's call. |
| [`proposals/project-reference-key.md`](./proposals/project-reference-key.md) | **Project reference key — the registry becomes the single source of truth for dispatch.** Night-2026-08-12 post-mortem: all three devclaw self-fix dispatches died on a stale registry path because the registry is *not on the dispatch path at all* — dispatch tools take a raw `workspace_dir` string with zero validation (callers can invent any path; it fails deep in the engine), registry reads are override-knob lookups joined by path, and `register_project` stores paths unvalidated. Direction: register once, reference by `project_id`, resolve `workspace_dir`/`repo_url`/knobs server-side at the one dispatch choke point; workspace-exists preflight at admission (not sandbox launch); write-time validation; resolution *populates* the path-keyed rows (joins unchanged in P1). Sliced P1 reference-key+preflight (~2 PRs) / P2 direct-path auto-prep / P3 id-keyed joins. | **DRAFT** — 2026-08-13; 4 `[OPEN]`s (deprecation path for raw paths, ad-hoc dispatch escape hatch, preflight failure verb, container/host path duality) mandatory before LOCK. **No code before lock** (tool-surface change). |
| [`proposals/preserve-failed-work.md`](./proposals/preserve-failed-work.md) | **Preserve failed work — a gate-failed task leaves a draft PR, not a vanished diff.** Night-2026-08-12: a real fix (23/23 tests green) was killed by a verify-gate structural false-negative and the diff survived only by hand-rescue before the next workspace reset. Ground truth: the diff outlives the sandbox (host bind-mount); it's the *next* `prepare_workspace` hard-reset that destroys it; delivery is gated on `success is not None` by scope, not invariant; `_pr_body` already renders an unreachable "gate did not pass" case. Direction: opt-in per-project `preserve_failed` flag → on qualifying terminal failure, push a clearly-named branch + open a **DRAFT PR** leading with the failure reason + gate output; the task **stays `failed`**, the PR never enters delivery-success surfaces, automerge never touches it (#183/#186 respected, not amended). Sliced P1 verify-gate failures (~2 PRs) / P2 crash-shaped (unreviewed — labeled) / P3 cycle-report surface. | **DRAFT** — 2026-08-13; 5 `[OPEN]`s (qualifying classes, branch hygiene/anti-accumulation, capture point + race, goal-layer visibility, per-goal override) mandatory before LOCK. **No code before lock** (delivery-contract change). |
| [`proposals/single-intake-doorway.md`](./proposals/single-intake-doorway.md) | **Single intake doorway — every ask, one owned pipeline, one durable receipt.** Triggered by the 2026-08-13 Ledger incident (a domain agent's A2A ask lived only in an ephemeral chat: no receipt, nothing durable, no may-act rule) — the class: intake has no contract. Direction: two stages as two privilege tiers of the one MCP surface. **Stage 1 (intent):** a new low-privilege `file_intake` tool — validates the intake shape in python at the choke point (Denys's rule: everything through a pipeline we own; gh is too generic), stamps provenance, files a `devclaw-intake` GitHub issue on the target *registered project*'s repo, returns the URL as the durable receipt; can only create issues, never dispatch. **Stage 2 (execution admission):** the existing dispatch tools, human-dispatcher-only; dispatch references the issue → PR `Closes #N` → the URL is the live status surface (pull ack — the asker follows up on its own cadence). Everything goes through stage 1, including Denys's own asks — every PR traces to an intake issue. Adopts issue-driven-pipelines §1 (Issues=intent, SQLite=execution); generalizes the self-issue-filing FILE edge; rides the project-reference-key registry chain. Sliced **P1 = `file_intake` + reference doc + lifekit-stack agent-instruction contract (~2 devclaw PRs + 1 lifekit-stack PR)** → P2 dispatch requires the issue ref (doorway unavoidable) → P3 push-notify asker + auto-pickup behind the gate. | **GRADUATED → [ADR 0012](./decisions/0012-single-intake-doorway.md)** — 2026-08-13, same day as the lock (all 5 `[OPEN]`s resolved one-by-one in-session; tranche scheduled immediately; P1 shipped as #513 + lifekit-stack#117). The ADR is canonical for the doorway decision; the proposal keeps the clarify trail and remains the home of P2/P3 (named-unsized, owner Denys). |

## Where the docs are NOT

- **The agent harness contract** — [`../CLAUDE.md`](../CLAUDE.md): the distilled
  working contract an agent reads before touching the repo. It deliberately
  duplicates the invariants in condensed form; `architecture.md` is the canonical
  statement.
- **The product narrative** — [`../README.md`](../README.md).
- **Worker-layer skills** — `.agent/skills/` (product, not harness docs).
- **Generated views** (`STATUS.md`/`log.md`/`deliveries.md` under goal dirs) —
  projections, never hand-edited, never audited here.
