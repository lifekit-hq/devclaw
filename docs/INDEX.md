# docs/ — index & currency map

Every doc under `docs/`, its one-line purpose, and a **currency tag** so a new
reader knows what to trust. Tags:

- **CURRENT** — verified against code; load-bearing claims hold.
- **DECISION RECORD** — a frozen ADR in `decisions/`: the decision stands, the
  point-in-time system descriptions are not maintained for drift.
- **STALE — see note** — contains at least one claim the code contradicts; note says what.

Currency is verified by grepping each doc's load-bearing claims against the code, not
by trusting the doc's own "Status:" line. When you change behavior that a doc
describes, fix the doc **and** update its tag here in the same PR.

## Layout

```
docs/
├── architecture.md    the system doc — mental model + the locked contract
├── flows/             temporal traces (one task; dispatch → PR)
├── reference/         look-up tables (env vars)
├── runbooks/          operational procedures (shakedown, VPS deploy)
├── decisions/         frozen ADRs — rationale, not current state
└── proposals/         pre-ADR staging — direction drafts (DRAFT → LOCKED → ADR)
```

## System

| Doc | Purpose | Currency |
|---|---|---|
| [`architecture.md`](./architecture.md) | **Start here.** Part I: the one-sitting mental model (five layers, two chains, the heartbeat, one task's journey, where state lives). Part II: the **locked contract** — per-layer contracts, the invariants (incl. grounded cognition, auto-heal), testability/replaceability, the question tree, the code map. | **CURRENT** — *written 2026-07-13* by merging `how-it-really-works.md` + `architecture-layers.md` (both deleted; invariants now stated once). Every load-bearing claim re-verified against main @ `8f9a59e` during the 2026-07-13 docs audit (5-agent sweep after the #228–#238 harden-loop tranche). *Updated 2026-07-15*: layer-1 tested-by row now names the telemetry read surface (`GET /traces.json` + `devclaw trace list/report`, pinned by `tests/test_trace_read_surface.py`); layer-3 gained an **operator-inspection** note for the `devclaw cognition plan\|decompose` dry-run (one cognition call, no docker/queue/state; `tests/test_cli_cognition.py`); layer-5 behavior row now describes the doctrine (always-on) vs `craft/` (self-selected, not concatenated) skill split. *Updated 2026-07-15*: "Where state lives" gained the **`problems` catalog** — the deduplicated self-observability capture layer (`StateStore.record_problem`, single writer, fingerprint UPSERT, wired at the block/task-fail/limit/trace choke points; `devclaw/state_store/problems.py`, `tests/test_problems_catalog.py`). *Updated 2026-07-16*: the `problems` catalog gained its **read surface** — the `list_problems` MCP tool (most-frequent first, optional `category` filter; `devclaw/server/tools.py`, `tests/test_list_problems_tool.py`). *Updated 2026-07-17*: mental-model gate step 5 now names the **browser-E2E gate** (`quality/browser_gate.py`) — a web-UI change must carry a passing real-browser Playwright run (parsed `browser_report` counts) or fail closed; flexible/strict per-project. *Updated 2026-07-18*: "Where state lives" gained the **self-triage** propose-only interceptor (`goal/triage.py`, layer-3 caller row + prompt `self-triage.md`) — the first consumer of the `problems` catalog, routing an eligible owner ping (allowlist: DB-size alarm) through a bounded dedupe+propose step; `tests/test_self_triage.py`. *Updated 2026-07-19*: layer-5 deps + replaceability-proofs row reflect the new `DEVCLAW_ACP_COMMAND` worker-agent config seam (seam tested; no second implementation exercised yet). *Updated 2026-07-19*: code map notes the quality gate is self-contained (own `quality/prompts/` + README; the 3 gate prompts moved out of `devclaw/prompts/`). *Updated 2026-07-18*: browser-E2E gate trigger scoped to **app surface** — a library-only diff (`*/src/lib/*`) is `not_triggered` (no app route to visit; proof = story+spec via the library build/test gate; evidence from a run that actually executed is still processed in full); `tests/test_browser_gate.py::test_browser_gate_library_only_diff_not_triggered`. *Updated 2026-07-19*: ONE planning spine (ADR 0003 stage 1) — `start_program`/the queue's `_planner` slot now route through the goal decomposer via the `plan_program` adapter (`planner.py`: `planned_from_checklist`, scaffold flag threaded to child task rows); the coarse `plan_goal`/`plan_spec` planners, their prompts, the `planner` model-tier row, and the `cognition plan` CLI are retired (`cognition decompose|breakdown` is the inspection surface); layer-3 operator-inspection note updated accordingly; `tests/test_planner.py`, `tests/test_queue_dag.py::test_program_child_task_row_carries_scaffold_flag`. *Updated 2026-07-19*: layer-2 contract gained the **execution dial** (ADR 0003 stage 2) — `Goal.mode: long_lived\|one_shot`; one-shot dispatches the whole checklist as ONE already-planned program (`Action.planned` → `start_planned_program`, `plan_key` child→item settle join), proposes done mechanically on drain, parks `needs_answer` on a not-achieved review; zero per-tick planner cognition (`tests/test_goal_one_shot.py`). *Updated 2026-07-20*: "Where state lives" gained the goal-transcending `project_docs` table — the **repo brief** workers hand back via the return contract's `REPO NOTES:` field, merged host-side at settle (plain line-dedupe, zero LLM) and prepended to future non-review dispatches on the same workspace (`goal/repo_brief.py`, `tests/test_repo_brief.py`). *Updated 2026-07-21*: "Where state lives" gained the **`eval_outcomes` projection** (ADR 0006, tranche PR1) — one row per task settle, materialized inside the settle commit by `mark_done`/`mark_failed`/`mark_task_cancelled` (single writer, exactly-once), `failure_class` by mechanical string bucketing (`state_store/rows.derive_failure_class`, zero LLM); basket reports ingest into the same table via `devclaw evals ingest` (`tests/test_eval_outcomes.py`). *Updated 2026-07-22*: gained the continuous-eval **run-cycle window-close report** (ADR 0006 decision 3, tranche PR2) — the goal heartbeat (scheduled-edge owner) fires a mechanical, zero-LLM report once per run-cycle close (nightly by default), assembling clean/wedge/pause from `eval_outcomes` + the `problems` catalog and pushing via the notifier; written to `cycle_reports` through the single-writer store (`goal/cycle_report.py`, `GoalService._maybe_emit_cycle_report`, `tests/test_cycle_report.py`). "night" was generalized to "cycle" (Denys, 2026-07-22) — the window is a recurring cycle, nightly only by default. *Updated 2026-07-22*: Part I gate step 5 + Part II single-writer invariant reflect the **gate strictness dial** (ADR 0007) — browser-E2E + adversarial-review gates are dial-able (advisory under a goal's default `trust`, fail-closed under `strict`; verify/test-integrity/done-gate stay always-hard, unreviewable cases still fail closed), and `set_goal_strictness` is the one blessed single-field mutation exception to "no `update_goal`". `CLAUDE.md`'s "Verification fails CLOSED" line carries the same recalibration note. *Updated 2026-07-23*: the `problems` catalog section now states the **gatherer/Issues split** (N1/#371, `issue-driven-pipelines` proposal) — GitHub Issues canonical for *intent*, SQLite canonical for *state*, the catalog a mechanical feeder; the `list_problems` MCP tool now carries each row's `issue_number`/`issue_state` + a derived `lifecycle` (single home `problems.problem_lifecycle`, shared with `/problems.json`) so it points at the canonical Issue instead of standing alone (`tests/test_list_problems_tool.py::test_list_problems_tool_points_each_row_at_its_canonical_issue`). |

## Flows

| Doc | Purpose | Currency |
|---|---|---|
| [`flows/task-execution.md`](./flows/task-execution.md) | Temporal trace of ONE task, every hop (node 1 waiter → node 2 devclaw-mcp → node 3 ephemeral sandbox), with a "fails if" rail. | **CURRENT** — *audited 2026-07-13*: all dispatch/settle symbols verified; Step I now lists the REPOSITORY CONTEXT input the review gate gained in #227. Sandbox mechanics re-checked 2026-07-11, unchanged. *Updated 2026-07-15*: Step D now names the per-task acceptance-criteria/constraints (carried inside the goal string) + the structured return contract `_wrap_goal` appends for code tasks. Step I now names `review_panel` + the opt-in `DEVCLAW_REVIEW_PANEL_N` diverse-lens panel (fail-closed on sub-quorum). *Updated 2026-07-19*: sandbox diagram shows `acp_command` from `DEVCLAW_ACP_COMMAND` (default `claude-agent-acp`). *Updated 2026-07-21*: Step C mounts the per-project toolchain cache volume; Step D opens with the fail-closed mise provisioning pre-step; deferred idea #4 marked superseded (all ADR 0005). *Updated 2026-07-22*: the sandbox binds a **pre-trusted** `.claude.json` copy (workspace-trust seeding — Claude Code now enforces per-workspace trust; `fix/claude-workspace-trust`). |
| [`flows/delivery.md`](./flows/delivery.md) | How dispatches become PRs: the 3 delivery shapes (backlog / checklist / program), the dispatch cap, the Shape-3 reconcile step. | **CURRENT** — *audited 2026-07-13*: cap-recovery row now names both human verbs (`steer_goal`/`resume_goal`, #228/#238) and notes the cap block never auto-heals; the Shape-1 `pr_state` quote fixed to match `tick_settle.py` verbatim. Cap formula re-verified (algebraically identical to `tick_dispatch.py`). |

## Reference

| Doc | Purpose | Currency |
|---|---|---|
| [`reference/env-vars.md`](./reference/env-vars.md) | Single source of truth for every env var the runtime reads, grouped by purpose. | **CURRENT** — *updated 2026-07-15*: added `DEVCLAW_COGNITION=agent_sdk` (opt-in agent-sdk cognition backend, feat/cognition-sdk-backend); `DEVCLAW_COGNITION_TIMEOUT_S` (cognition-timeout headroom, default 180s replacing the hardcoded 90s cap — now also the agent_sdk inactivity budget); `DEVCLAW_TRACE_RETENTION_DAYS` (trace-retention prune); `DEVCLAW_REVIEW_PANEL_N` (opt-in diverse-lens review panel, default 1 = single reviewer). Prior audit 2026-07-13 (#228–#238 tranche; `DEVCLAW_SKILL_LIBRARY` removed). Doc↔code parity enforced by `tests/test_env_vars_doc_sync.py` (bidirectional). *Updated 2026-07-17*: added `DEVCLAW_GOAL_BROWSER_GATE` + `DEVCLAW_GOAL_BROWSER_GATE_MODE` (the browser-E2E gate on/off + flexible/strict). *Updated 2026-07-18*: added `DEVCLAW_GOAL_BROWSER_REACHABILITY` (reasoned, grounded, fail-closed escape valve — clears the browser gate only when an independent judge proves the changed UI isn't rendered in the running app); added `DEVCLAW_EVENTS_RETENTION_DAYS` (events-table retention prune, volume hygiene — the `events` log now bounded like `traces`); added `DEVCLAW_DB_SIZE_ALERT_MB` (loud-not-silent DB-size owner alarm, default 2000MB — pairs with the new weekly VACUUM). *Updated 2026-07-18*: added `DEVCLAW_SELF_TRIAGE` (propose-only self-triage interceptor, default on — an eligible owner ping is routed through a bounded triage cognition step that dedupes against the `problems` catalog + drafts a proposed fix; slice-1 allowlist = the DB-size alarm; `tests/test_self_triage.py`). *Updated 2026-07-18*: added `DEVCLAW_ITEM_MAX_ATTEMPTS` (structural per-checklist-item circuit breaker, default 3 — after N straight failed settles the item flips to `blocked` and the goal is parked `needs_human`, replacing the planner-authored "CIRCUIT BREAKER" prose; `tests/test_goal_tick_checklist.py`). *Updated 2026-07-18*: added `DEVCLAW_REVIEW_DEGRADE` + `DEVCLAW_REVIEW_DEGRADE_MAX_FILES` (the review gate's cognition-timeout degradation ladder — on a whole-diff timeout, split per file, review each, union the verdicts; fail-closed preserved, exhaustion re-raises to the same #186 crash path; `tests/test_review_panel.py`). *Updated 2026-07-19*: added `DEVCLAW_ACP_COMMAND` (the worker-agent replaceability seam — swap the in-sandbox ACP agent command without a code change; payload-threaded like `model`, shlex-split by the runner, default `claude-agent-acp`; `tests/test_acp_command_config.py`). *Updated 2026-07-19*: `DEVCLAW_MODEL_DEEP` row no longer lists `plan_goal` (retired — programs plan through the decomposer, ADR 0003 stage 1). *Updated 2026-07-20*: added `DEVCLAW_ITEM_ASSERTS` (reality-anchored acceptance asserts, #2/#4 — a checklist item's mechanical `file_exists`/`grep` asserts are enforced against the delivered workspace at settle; an item can't flip to `done` if they don't hold, fail-closed into the retry/breaker path; default on, kill-switch for a mis-authored assert; `tests/test_item_asserts.py`). |

## Runbooks

| Doc | Purpose | Currency |
|---|---|---|
| [`runbooks/live-shakedown.md`](./runbooks/live-shakedown.md) | Exercising the real pipeline (logged-in `claude` + docker) layer by layer, L1 single task → L5 abort. | **CURRENT** — *audited 2026-07-13*: every tool named in the steps exists on the MCP surface; L1–L5 descriptions match. *Updated 2026-07-19*: L2/L3 rewritten for the start_program → one-shot-goal alias (ADR 0003 stage 2b) — follow via `get_goal`/`tail_goal`, the child program appears in `list_programs` after the heartbeat dispatches the checklist. *Updated 2026-07-19 (post-shakedown)*: L2 workspace setup now requires a host-visible origin (goal-layer prep fetches it) and notes a /tmp file remote validates mechanics only — a green close needs a real pushable remote (sandbox can't push host file paths). |
| [`runbooks/vps-waiter-deploy.md`](./runbooks/vps-waiter-deploy.md) | Deploying the OpenClaw waiter + devclaw to the VPS; the waiter's tool menu. | **CURRENT** — *audited 2026-07-13*: all menu tools exist; `resume_goal` added to the Goals line (#228). *Updated 2026-07-19*: `start_program` menu line marked as the deprecated one-shot-goal alias (ADR 0003 stage 2b — poll with `get_goal`, not `get_program`). |

## Decision records (frozen — rationale, not current state)

| Doc | Purpose | Currency |
|---|---|---|
| [`decisions/0001-openhands-engine.md`](./decisions/0001-openhands-engine.md) | The founding decision: adopt OpenHands as the execution engine; devclaw is thin orchestration. Includes the OpenHands-vs-isolation orthogonality rationale and why devclaw calls `docker run` itself. | **DECISION RECORD** — accepted 2026-06, frozen 2026-07-13 (was `architecture-v2.md`). The decision stands; system snapshots inside reflect their writing date. Current system: [`architecture.md`](./architecture.md). |
| [`decisions/0002-engine-mode.md`](./decisions/0002-engine-mode.md) | The OpenHands-vs-`claude_sdk` engine choice: current default, the switch procedure, and the data a switch requires. | **DECISION RECORD** — frozen 2026-07-13 (was `engine-decision.md`). Default (`run_sandcastle`) and `DEVCLAW_ENGINE` values re-verified at freeze time. |
| [`decisions/0003-goal-program-unification.md`](./decisions/0003-goal-program-unification.md) | Goal ↔ program unification: ONE `create_goal`, one dial (re-evaluation cadence — one-shot vs long-lived), one planning spine (grill→firm→decompose), shippable-release iterations, two-level sizing rule, fog-of-war scope map, checkpoint grill + auto-continue, hard cost backstops. Staged migration plan (PR-B → surface collapse → iterative loop). | **DECISION RECORD** — accepted 2026-07-19. Design locked; implementation staged — the "today" descriptions inside describe the pre-migration system and will drift as stages land. |
| [`decisions/0004-eval-workbench.md`](./decisions/0004-eval-workbench.md) | Eval-driven guardrail proportioning + the living eval workbench: stop hardening blind — measure with the existing instrument (`measure_passrate` + judge), rebalance Python guardrails toward context/prompt engineering, calibrate every shed to the WEAKEST target model (the 2×2; never delete on the Claude column alone), surface run history as a console "Evals" tab over an `eval_runs` table. Phased: Claude baseline → workbench MVP → judge-as-scorer → shed-candidate ID → local-model calibration + shedding LAST. | **DECISION RECORD** — accepted 2026-07-20 (graduated from `proposals/eval-workbench.md` under the spec lifecycle). Implementation phased; step-1 execution started 2026-07-20. **Step 2 amended 2026-07-21 by [ADR 0006](./decisions/0006-continuous-eval-projection.md)** (two-source outcome projection replaces the basket-only `eval_runs`). |
| [`decisions/0005-generic-sandbox-toolchain.md`](./decisions/0005-generic-sandbox-toolchain.md) | Kill the per-stack sandbox image family: one lean harness image; the toolchain is a project-declared fact (`.mise.toml` / `.tool-versions` / `global.json` / `package.json` engines) provisioned by mise in a fail-closed runner pre-step, cached in a per-project docker volume. Per-project `sandbox_image` override = escape hatch + migration bridge; greenfield goals get the declaration file as the first checklist item. | **DECISION RECORD** — accepted 2026-07-21 (graduated from the vault proposal `2026-07-11-generic-sandbox-project-declared-toolchain` under the spec lifecycle). Tranche COMPLETE 2026-07-21: gate passed live (finance-sentry#291 green on the lean image + mise path) and `Dockerfile.dotnet` is deleted — the "current family" snapshot inside reflects its writing date. |
| [`decisions/0006-continuous-eval-projection.md`](./decisions/0006-continuous-eval-projection.md) | Continuous evaluation as a projection of the live event stream: every live task settle writes an `eval_outcomes` row at the settle commit (zero extra tokens); basket runs ingest into the same table as `source=basket`; clean-cycle rate (locked boundary: self-healed pause = clean-but-reported) as the second headline metric; a mechanical window-close push report via the scheduled-edge owner (heartbeat today) → existing notifier; `measure_passrate` demoted to the experiment tool with an automatic 1-ticket post-deploy smoke. Amends ADR 0004 step 2 only. "night"→"cycle" vocabulary generalized 2026-07-22 (see the ADR's Naming note). | **DECISION RECORD** — accepted 2026-07-21 (graduated from `proposals/continuous-eval-projection.md`; drafted, clarified, locked, and graduated the same evening). Tranche shipped 2026-07-22. |
| [`decisions/0007-gate-strictness-dial.md`](./decisions/0007-gate-strictness-dial.md) | Gate strictness becomes a per-goal dial (`trust\|strict`, default `trust`): dial-able gates (browser-E2E, adversarial-review) default to **advisory** — log loudly + surface in the PR + count in `eval_outcomes`, but ship rather than wedge; the human merge is the strict backstop. Three gates stay always-hard (test-integrity, delivery-trust, done-gate). Recalibrates — does not repeal — "loud failure over silent degradation". Impl shape: `Strictness` enum + ONE pure `(gate_id, verdict, strictness)→block\|advise` policy function, NOT mode-objects; Strategy seam reserved for the deferred judge-gate. Subsumes `DEVCLAW_GOAL_BROWSER_GATE_MODE`. | **DECISION RECORD** — accepted 2026-07-22 (graduated from `proposals/gate-strictness-dial.md`; drafted, clarified, locked, and graduated the same day). Tranche in flight 2026-07-22. |
| [`decisions/0008-console-operator-surface-p1.md`](./decisions/0008-console-operator-surface-p1.md) | Console operator surface **P1**: a read-only drill-down spine `node → project → goal → milestone(plan_key) → task` with active-shown/settled-folded disclosure at every tier + a layer-trace view — over projections that already exist (layer-1/dashboard only; no writes, no tick-path cognition). Milestone = `plan_key` grouping (a view, not an entity); P1 is read/navigate-only (mutation → P3). Net-new: `GET /node.json` vitals + surfacing `plan_key`/`milestone` in `_task_row` + the NODE/milestone/layer-trace frontend; reuses `GoalDetail`. Sized ≈6 PRs, end-of-week cap. | **DECISION RECORD** — accepted 2026-07-23 (graduated the P1 slice from `proposals/console-operator-surface.md`); **SHIPPED** PRs A–F (#357/#360/#361/#363/#364/#365). |
| [`decisions/0009-console-operator-surface-p2.md`](./decisions/0009-console-operator-surface-p2.md) | Console operator surface **P2**: the health enrichment + the **problem-lifecycle tracker**. Net-new `GET /problems.json` over the `problems` catalog (surfacing `issue_number`/`issue_state`) + a Problems screen rendering each problem *identified → filed → open → resolved* (self-issue-filing Stage 1 data); plus error-class (`failure_class`) breakdown + estimated token-spend on the existing Evals surface. Resolves §5.5 (fix stage shown **gated**, propose-only) + §5.6 (token spend **estimated**). Read-only, layer-1. Sized ≈2 PRs. | **DECISION RECORD** — accepted 2026-07-23 (graduated the P2 slice after P1 shipped); **SHIPPED** P2-A/P2-B (#367/#368). |
| [`decisions/0010-console-p3-structured-blocks.md`](./decisions/0010-console-p3-structured-blocks.md) | Console **P3.1 (§6)**: structured decision blocks. When the planner blocks `needs_answer`, its EXISTING cognition call also emits `{options:[{key,label,detail,steer}], recommended, allow_custom}`; options persist as a per-goal block-options doc (firming-unknowns precedent, no schema change), surface in `/goals/{id}.json`, and the console renders click-to-steer buttons reusing the EXISTING steer POST. No new LLM call, no new mutation/auth surface (§6a already solved). Resolves §6a–§6d. Layer-3 cognition change; blank-safe so absent options = today's behavior. Sized ≈2 PRs (P3.1a cognition+persistence+wire; P3.1b render). | **DECISION RECORD** — accepted 2026-07-23. **P3.2 (co-pilot chat + waiter-over-OAuth §4 + §5.3 chat-scope) deferred**, still in the proposal. |

## Proposals (pre-ADR staging — the status line inside the doc is authoritative)

The spec lifecycle ([`.claude/rules/spec-lifecycle.md`](../.claude/rules/spec-lifecycle.md)):
a direction is drafted here, every `[OPEN]` item is answered (the clarify
step), the status flips to LOCKED, and when a tranche is scheduled the
proposal graduates to a frozen ADR in `decisions/`. No code before lock.
The tag below mirrors the doc's own status line.

| Doc | Purpose | Status |
|---|---|---|
| [`proposals/eval-workbench.md`](./proposals/eval-workbench.md) | Eval-driven guardrail proportioning + the living eval workbench: measure the loop with the existing scored instrument (`evals/measure_passrate.py` + the judge), rebalance Python guardrails vs context/prompt engineering against the *weakest target model* (small local — the 2×2 classification), and make the system's evolution visible as a console "Evals" surface backed by an `eval_runs` table. Phased: Claude baseline → workbench MVP → judge-as-scorer → shed-candidate ID → local-model calibration + authorized shedding LAST. | **GRADUATED → [ADR 0004](./decisions/0004-eval-workbench.md)** — 2026-07-20 (tranche scheduled; packaging borrow completed same day). The ADR is canonical; the proposal keeps the full narrative + clarify-step trail. |
| [`proposals/continuous-eval-projection.md`](./proposals/continuous-eval-projection.md) | Continuous evaluation as a projection of the live event stream: every live task settle is an eval sample (zero extra tokens); one outcome projection with a `source: live\|basket` column reshapes ADR 0004 step 2's `eval_runs`; clean-night rate as the second headline metric; a mechanical window-close push report through the notifier; `measure_passrate` demoted to the controlled-A/B experiment tool. No LLM added to any tick path. | **GRADUATED → [ADR 0006](./decisions/0006-continuous-eval-projection.md)** — 2026-07-21 (drafted, clarified, locked, tranche scheduled, and graduated the same evening). The ADR is canonical; the proposal keeps the full narrative + the seven clarify-step resolutions. |
| [`proposals/gate-strictness-dial.md`](./proposals/gate-strictness-dial.md) | Gate strictness becomes a per-goal dial: gates default to **advisory** (log loudly, surface in the PR, do not wedge), `strict` is an opt-in for goals whose output the owner depends on (finance-sentry). Recalibrates "loud failure over silent degradation" — loud stays, "failure" stops meaning "wedge"; the human merge is the strict backstop. Subsumes `DEVCLAW_GOAL_BROWSER_GATE_MODE`. Names the adversarial *judge-gate* + the Playwright-sandbox timeout fixes as explicit deferred graduations. | **GRADUATED → [ADR 0007](./decisions/0007-gate-strictness-dial.md)** — 2026-07-22 (drafted, clarified, locked, and graduated the same day; all 5 `[OPEN]` items resolved by Denys in §5). The ADR is canonical; the proposal keeps the narrative + clarify-step trail + the impl-shape rationale (§9). |
| [`proposals/ops-agent-problems-consolidation.md`](./proposals/ops-agent-problems-consolidation.md) | Ops-agent ⇄ continuous-eval boundary. Proposed routing ops-agent incidents into devclaw's `problems` catalog — **abandoned**: that subordinates a generic cross-project watchdog to one target. The useful residue is the north star it surfaced: two *types* of watchdog must stay separate — **infra-ops** (the ops-agent's true job, generic) vs the **dev-loop** (devclaw). The ops-agent has drifted into the dev-loop (O1/O3 watch goal progress, `evaluate_goal`); fix = subtract it back to infra, fold goal-poking into devclaw's own auto-heal. | **ABANDONED** — 2026-07-22, same day. Backwards direction; kept for the overlap analysis + the infra-vs-dev-loop distinction. No replacement scheduled. |
| [`proposals/self-issue-filing.md`](./proposals/self-issue-filing.md) | The **self-improving cycle** (GATHER→FILE→FIX→CLOSE): devclaw turns a *recurring* problem into a labeled GitHub issue on its own repo, and — later, gated — picks it up as a goal to fix through the existing gates; issues leave the board when fixed or aged-out (anti-accumulation). Dev-loop half of the ops-agent cut; rescues the dead O4 trend-repeat detector as the recurrence trigger. Sliced: **P1 file+close** (mechanical, zero-LLM, no self-merge) → **P2 fix** → P3 tighten. | **Stage 1 (FILE+CLOSE) LOCKED** — 2026-07-22; clarify O1–O8 resolved. **Stage 2 (FIX) LOCKED 2026-07-23** — §3 self-merge autonomy = **tiered by blast radius** (Denys reversed his full-auto lean; `quality/`+gate-prompts added to the human-gated core); **P2 sliced propose-only** (fix→PR→human merges, ≈1–2 PRs) — tiered classifier DEFERRED to P2.1 (shadow) / P2.2 (live) as a complexity cut, §3 stays the target; P3 unsized. Backlog #340. |
| [`proposals/v1-helper-resurface.md`](./proposals/v1-helper-resurface.md) | **Re-surface the v1 task-runner helper** (bounded task → PR, no goal ceremony) — reconciles the helper→PoC drift + removes the `speckit-handoff-gap` wall. Grounded finding: the direct runner (`dispatch_task`) is fully wired and works; it's just **unsurfaced** and can only branch a fresh name off `main` → PR to `main`. Base=`main` lives in exactly 2 spots and the branch-reuse capability already exists (gated to `goal/…`); the seam was pre-planned (`delivery_strategy.py`, `PerActionStrategy`). Sliced **P1 = the branch-target delivery seam** (`base_branch` + `target_branch`, continue an existing branch, PR to a chosen base) → **P2 = console "file a task" surface + docs** → **P3 = direct-path workspace-prep ergonomics**. | **P1 LOCKED (direction)** — 2026-07-23; all P1 `[OPEN]`s resolved (§7), P2/P3 deferred-with-owner. LOCKED = direction not schedule; tranche is Denys's to sequence. P1 shape ≈2–3 PRs, end-of-week cap. |
| [`proposals/issue-driven-pipelines.md`](./proposals/issue-driven-pipelines.md) | **Issue-driven continuous development** — GitHub Issues as the control plane, devclaw as the engine. Two non-overlapping intake threads onto one engine: **goals** (durable, build-a-product) vs a **per-repo queue** (bounded units = labeled GitHub Issues → picked up → PR → closed). One source of truth split by *kind*: Issues canonical for **intent/backlog**, SQLite canonical for **execution state** (Issues have no CAS — never the transactional store). Governance = an **in-repo `.devclaw/` manifest** (opt-in, inert, portable — the repo lives without devclaw; presence = the allowlist) + thin webhook/subscription discovery. Generalizes the existing self-issue-filing pickup from the devclaw repo to any owned repo (first external = finance-sentry). **Deliberately split after an honest scope review (§0.5):** the grand control-plane vision (manifest, webhooks, multi-repo) is **DEFERRED — recorded, unscheduled** (revive only after the reliability wall is down; done early it would mass-produce the subtly-wrong PRs that got finance-sentry-ui cancelled). Only a small **NOW slice** is live work: **N1** collapse `problems`→Issues (source-of-truth hygiene) + **N2** make the existing self-issue-filing loop legible in the console. | **DRAFT (split NOW/DEFERRED)** — 2026-07-23; captured from a 4-message design conversation, then scope-reviewed. NOW = N1/N2 (bounded, out of lock scope). DEFERRED arc keeps 8 `[OPEN]`s (§7), mandatory only if revived. |
| [`proposals/console-operator-surface.md`](./proposals/console-operator-surface.md) | The console as a **live operator surface**, not a status board: the running node visible from the top, drilled down through `node → project → goal → milestone(slice) → task` with active-shown/settled-folded disclosure at every tier, a layer trace that catches errors in place, a health/self-evaluation dashboard (`eval_outcomes` + `cycle_reports` charts), a **problem-lifecycle tracker** (identified→filed→fixing→resolved), and a **co-pilot chat** that commands it all — over the *existing waiter*/OAuth (§4), via an intent API, not computer-use. Plus (§6) **structured decision blocks**: a `needs_answer`-blocked goal renders as options + a recommendation + custom-answer, click→`steer_goal` (generalizes firming's `answer_unknowns` to execution). Packaging/legibility-primary, with a `[OPEN] #7` lean toward a commandable daily driver (P2/P3 direction). | **P1 → [ADR 0008](./decisions/0008-console-operator-surface-p1.md) (SHIPPED); P2 → [ADR 0009](./decisions/0009-console-operator-surface-p2.md) (SHIPPED); P3.1 §6 → [ADR 0010](./decisions/0010-console-p3-structured-blocks.md) (graduated)** — 2026-07-23; all clarify items resolved. Reference mockup is a demo, not the target IA (§5.8). P1 read-hierarchy + P2 health/problems shipped; **P3.1 = §6 structured decision blocks** (ADR 0010, ≈2 PRs); **P3.2 = co-pilot chat + waiter-over-OAuth (§4) + §5.3 chat-scope** stays named-unsized, LOCKED-direction here. |

## Where the docs are NOT

- **The agent harness contract** — [`../CLAUDE.md`](../CLAUDE.md): the distilled
  working contract an agent reads before touching the repo. It deliberately
  duplicates the invariants in condensed form; `architecture.md` is the canonical
  statement.
- **The product narrative** — [`../README.md`](../README.md).
- **Worker-layer skills** — `.agent/skills/` (product, not harness docs).
- **Generated views** (`STATUS.md`/`log.md`/`deliveries.md` under goal dirs) —
  projections, never hand-edited, never audited here.
